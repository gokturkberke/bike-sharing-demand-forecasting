# Phase 0 dependencies with major-version upper bounds.
# Pin to exact versions once the full pipeline is validated (Phase 6+).
pandas>=2.0,<4
numpy>=1.24,<3
scikit-learn>=1.3,<2
pyyaml>=6.0,<7
matplotlib>=3.7,<4
seaborn>=0.12,<1
jupyter>=1.0,<2
pyarrow>=14.0,<25
# xgboost also needs the OpenMP system runtime (macOS: `brew install libomp`).
xgboost>=2.0,<4
pytest>=7.4,<10
